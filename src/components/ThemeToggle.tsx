
// This component is no longer used since we removed dark mode

import { Button } from "@/components/ui/button";

export function ThemeToggle({ className }: { className?: string }) {
  return null;
}
